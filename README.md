# data ブランチ

DAILY REPS の記録の置き場所です。アプリからの自動同期がここに `state.json` を書きます。

アプリ本体（`index.html` など）は `main` にあります。
記録をこちらに分けているのは、保存のたびに GitHub Pages が再ビルドされるのを避けるためです。

`state.json` は手で編集しないでください（アプリが上書きします）。
中身を戻したいときは、アプリの「バックアップを読み込む」を使ってください。
